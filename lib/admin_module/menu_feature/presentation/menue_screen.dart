import 'package:flutter/cupertino.dart';
import 'package:side_proj/admin_module/menu_feature/presentation/menu_screen_impl.dart';

class AdminMenueScreen extends StatelessWidget {
  const AdminMenueScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MenuScreenImpl();
  }
}
