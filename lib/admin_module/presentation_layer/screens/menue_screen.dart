import 'package:flutter/cupertino.dart';

class AdminMenueScreen extends StatelessWidget {
  const AdminMenueScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
