import 'package:flutter/cupertino.dart';

class AdminTutorialScreen extends StatelessWidget {
  const AdminTutorialScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
